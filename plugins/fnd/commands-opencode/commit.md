---
# GENERATED by scripts/gen-host-adapters.cjs — do not edit; change the source and re-run.
# source: skills/commit/SKILL.md
description: "Create git commits per the Conventional Commits spec. Use when the user asks to commit changes, write a commit message, or run git commit."
---

Load and follow the fnd `commit` skill, with $ARGUMENTS as its input.

Load it by name through the host's skill lookup. If that does not resolve, read the skill body directly: `<plugin root>/skills/commit/SKILL.md`, where **plugin root** = the plugin's own directory — this shim is `<plugin root>/commands-opencode/commit.md`, symlinked into the OpenCode config dir by the installer.
