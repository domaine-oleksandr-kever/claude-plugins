---
# GENERATED by scripts/gen-host-adapters.cjs — do not edit; change the source and re-run.
# source: skills/write-steps-to-test/SKILL.md
description: "Write Steps to Test for a Jira ticket in Domaine's standard format — maps each AC to reproducible scenarios for a QA engineer unfamiliar with the implementation; updates the Jira field after approval — Workflow 5. Use when the user asks to write / draft Steps to Test or QA steps for a Jira ticket."
---

Load and follow the fnd `write-steps-to-test` skill, with $ARGUMENTS as its input.

Load it by name through the host's skill lookup. If that does not resolve, read the skill body directly: `<plugin root>/skills/write-steps-to-test/SKILL.md`, where **plugin root** = the plugin's own directory — this shim is `<plugin root>/commands-opencode/write-steps-to-test.md`, symlinked into the OpenCode config dir by the installer.
