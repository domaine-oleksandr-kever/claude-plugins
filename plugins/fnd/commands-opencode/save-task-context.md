---
# GENERATED by scripts/gen-host-adapters.cjs — do not edit; change the source and re-run.
# source: skills/save-task-context/SKILL.md
description: "Create or update the task workspace (`.claude/tasks/<work-id>/`) from what is already in the conversation — ticket fields, decisions, root causes, progress — so the next skill or a fresh session resumes without re-running readers. Use when the user asks to save / remember the task context or progress (сохранить контекст) or set up a task workspace."
---

Load and follow the fnd `save-task-context` skill, with $ARGUMENTS as its input.

Load it by name through the host's skill lookup. If that does not resolve, read the skill body directly: `<plugin root>/skills/save-task-context/SKILL.md`, where **plugin root** = the plugin's own directory — this shim is `<plugin root>/commands-opencode/save-task-context.md`, symlinked into the OpenCode config dir by the installer.
