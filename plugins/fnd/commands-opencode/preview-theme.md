---
# GENERATED by scripts/gen-host-adapters.cjs — do not edit; change the source and re-run.
# source: skills/preview-theme/SKILL.md
description: "Create or refresh an unpublished Shopify PREVIEW theme from the current branch. Use when the user asks to create / make / spin up a preview theme, test the preview script, or update / refresh / redeploy / rebuild a preview / push a fix to one. A bare theme id means refresh that theme."
---

Load and follow the fnd `preview-theme` skill, with $ARGUMENTS as its input.

Load it by name through the host's skill lookup. If that does not resolve, read the skill body directly: `<plugin root>/skills/preview-theme/SKILL.md`, where **plugin root** = the plugin's own directory — this shim is `<plugin root>/commands-opencode/preview-theme.md`, symlinked into the OpenCode config dir by the installer.
